<% if @feed_items.any? %>
  <ol class="questions">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :communities, action: :show } %>
<% end %>
