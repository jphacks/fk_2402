<% unless @community.creator_id == current_user.id %>
  <div id="follow_form">
  <% if current_user.following?(@community) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>